server:
  port: 8082
  servlet:
    context-path: /panierapi

spring:
  devtools:
    restart:
      enabled: 'true'
      poll-interval: '2s'
      quiet-period: '1s'
      additional-paths:
      - src/main/kotlin/
